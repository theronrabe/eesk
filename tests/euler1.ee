#This program solves projecteuler's first problem
{
	#is multiple of 3 or 5?
	Set isMul {n} {
		if {(n$%3)!|(n$%5)!} {return 1} {return 0}
	}
Begin
	while {i$ < 1000} {
		if {isMul[i$]} {
			sum = sum$+i$;
		}{}
		i = i$+1;
	}
	sum$;
End
}
