#This program tests recursion
{
	public Function fib {N} {
		if {N$<2} {
			return 1;
		}{
			return (fib[N$ - 2]) + (fib[N$ - 1]);
		}
	}
Begin
	while {loop$ < 40} {
		printd fib[loop$];
		loop = loop$ + 1;
	}
End
}
