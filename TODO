Compiler to-do list:
	-command line options
		-include path
		-verbose mode
	
Virtual Machine to-do list:

Eesk Environment to-do list:
	-Need a new syntax for *8 for continuation element access
	-Need a predictable means of applying evaluation to non-Set elements
		-"100 []" should evaluate to 100, not produce a segfault
	-Continue work on manual
	-Add x86-64 support for floating point values

Currently working on:
